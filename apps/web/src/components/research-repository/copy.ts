export const RESEARCH_REPOSITORY_TRUST_COPY =
  "Disconnect or uninstall deletes tokens stored here. GitHub keeps the repository under its own control. Reconnecting is a new grant. If the repo is deleted, recreate or re-add it and re-grant access. If a branch is deleted or force-pushed, recover it on GitHub, then reconcile or reseed from a sealed layout.";

export const REPOSITORY_UNAVAILABLE_COPY =
  "Repository unavailable (deleted or access removed). Restore by recreating or re-adding the repository, then re-grant the GitHub App. For a deleted or force-pushed branch, recover it on GitHub, then reconcile — or reseed from a sealed layout.";

export const REPOSITORY_PUBLIC_COPY =
  "Read-only: repository is public. Writes are disabled until the repository is private again.";

export const REPOSITORY_AUTHORIZATION_COPY =
  "GitHub authorization is required. Re-authorize GitHub to restore repository access. Current-session local content is not synced / last locally available state.";

export const REPOSITORY_DISCONNECTED_COPY =
  "GitHub is disconnected. Reconnect GitHub to restore repository access. Current-session local content is not synced / last locally available state.";

export const REPOSITORY_PERMISSION_COPY =
  "GitHub permission was lost for this repository. Reconnect GitHub after restoring access. The workspace remains readable, but writes are disabled.";
